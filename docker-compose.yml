services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: job_agent
      POSTGRES_USER: job_agent
      POSTGRES_PASSWORD: job_agent
    ports:
      - "5432:5432"
    volumes:
      - job_agent_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U job_agent -d job_agent"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  job_agent_pgdata:
